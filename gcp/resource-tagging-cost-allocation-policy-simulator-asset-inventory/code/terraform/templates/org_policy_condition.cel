# Organization policy condition to enforce mandatory resource labels
# This CEL expression requires all specified labels to be present on resources

%{~ for label in mandatory_labels ~}
has(resource.labels.${label})%{~ if label != mandatory_labels[length(mandatory_labels)-1] ~} &&
%{~ endif ~}
%{~ endfor ~}
# AWS CDK Python Dependencies for AWS CLI Tutorial
# Pin to latest stable CDK v2 versions for consistency and security

# Core CDK library - required for all CDK applications
aws-cdk-lib==2.172.0

# Constructs library - provides base constructs for CDK
constructs>=10.0.0,<11.0.0

# Additional CDK modules used in this application
# (These are included in aws-cdk-lib but listed for documentation)

# Development and testing dependencies
pytest>=7.0.0,<8.0.0
pytest-cov>=4.0.0,<5.0.0

# Code quality and formatting
black>=23.0.0,<24.0.0
flake8>=5.0.0,<7.0.0
mypy>=1.0.0,<2.0.0

# Type stubs for better IDE support and type checking
types-setuptools>=68.0.0
boto3-stubs[s3,iam,sts]>=1.26.0

# Optional: AWS CLI for testing deployments locally
# awscli>=2.0.0

# Documentation generation (optional)
# sphinx>=5.0.0,<8.0.0
# sphinx-rtd-theme>=1.0.0,<2.0.0
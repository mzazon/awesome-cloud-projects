# AWS CDK Python Dependencies for Multi-Cluster EKS with Cross-Region Networking

# Core CDK library
aws-cdk-lib>=2.100.0,<3.0.0

# Construct library
constructs>=10.0.0,<11.0.0

# Python type checking
typing-extensions>=4.0.0

# Additional CDK modules for specific AWS services
# Note: These are included in aws-cdk-lib but listed for reference
# aws-cdk.aws-ec2
# aws-cdk.aws-eks
# aws-cdk.aws-iam
# aws-cdk.aws-logs
# aws-cdk.aws-route53
# aws-cdk.aws-vpclattice

# Development and testing dependencies (optional)
pytest>=7.0.0
pytest-cov>=4.0.0
black>=22.0.0
flake8>=5.0.0
mypy>=1.0.0

# AWS CLI for deployment and management
awscli>=1.29.0

# kubectl for Kubernetes cluster management
# Note: kubectl should be installed separately via package manager or binary download

# eksctl for EKS cluster management
# Note: eksctl should be installed separately via package manager or binary download

# Helm for installing charts
# Note: helm should be installed separately via package manager or binary download
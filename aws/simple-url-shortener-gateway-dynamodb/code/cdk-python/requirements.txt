# AWS CDK Python dependencies for URL Shortener application
# This file specifies all required packages and their versions

# Core AWS CDK library
aws-cdk-lib==2.170.0

# CDK constructs library (required for CDK v2)
constructs>=10.0.0,<11.0.0

# Additional AWS CDK libraries for specific services
# (These are included in aws-cdk-lib for CDK v2, but listed for reference)

# Development dependencies (optional, for testing and linting)
# Uncomment these if you want to add testing and code quality tools
# pytest>=7.0.0
# boto3-stubs[essential]>=1.26.0
# mypy>=1.0.0
# black>=22.0.0
# flake8>=5.0.0
# pylint>=2.15.0

# Production dependencies for Lambda functions
# Note: These are embedded in the Lambda function code, not CDK dependencies
# boto3 - Already available in Lambda runtime
# botocore - Already available in Lambda runtime
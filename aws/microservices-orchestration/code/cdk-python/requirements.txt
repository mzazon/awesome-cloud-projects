# AWS CDK Python Dependencies for Event-Driven Microservices
# This file specifies all required Python packages for the CDK application

# Core CDK library - provides fundamental CDK constructs and functionality
aws-cdk-lib==2.115.0

# CDK Constructs library - higher-level constructs for common patterns
constructs>=10.0.0,<11.0.0

# Python standard library dependencies for type checking
typing-extensions>=4.8.0

# Optional development dependencies for better development experience
# Uncomment these for development and testing

# pytest>=7.4.0              # Testing framework
# pytest-cov>=4.1.0          # Coverage reporting
# black>=23.9.0               # Code formatting
# flake8>=6.0.0               # Linting
# mypy>=1.5.0                 # Type checking
# boto3-stubs[essential]      # Type stubs for boto3

# Production deployment dependencies
# These are required for Lambda function runtime

# boto3>=1.29.0               # AWS SDK for Python (already available in Lambda runtime)
# botocore>=1.32.0            # Core library for boto3 (already available in Lambda runtime)

# Note: boto3 and botocore are pre-installed in AWS Lambda Python runtime
# They are commented out here to avoid version conflicts during deployment
# If you need specific versions, uncomment and specify the required versions
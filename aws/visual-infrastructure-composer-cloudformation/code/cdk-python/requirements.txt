# AWS CDK Python Requirements
# Visual Infrastructure Design with Application Composer and CloudFormation

# Core CDK dependencies
aws-cdk-lib==2.167.1
constructs>=10.0.0,<11.0.0

# CDK specific construct libraries
# Note: aws-cdk-lib includes all core services, but individual imports may be needed
# for specialized constructs or custom resources

# Development and testing dependencies (optional)
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.7.0
flake8>=6.0.0
mypy>=1.5.0

# Type checking for boto3 (useful for Lambda functions and custom resources)
boto3-stubs[essential]>=1.26.0

# Additional utilities for CDK development
jsii>=1.84.0
publication>=0.0.3
typeguard>=4.0.0

# AWS SDK for any custom resource implementations
boto3>=1.26.0
botocore>=1.29.0

# Development tools for better code quality
pre-commit>=3.3.0
bandit>=1.7.5  # Security linting

# Documentation generation (optional)
sphinx>=7.1.0
sphinx-rtd-theme>=1.3.0

# Environment management
python-dotenv>=1.0.0
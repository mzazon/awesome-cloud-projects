# AWS CDK Python dependencies for Multi-VPC Transit Gateway Architecture
# These packages provide the necessary AWS CDK constructs and utilities

# Core CDK framework and common constructs
aws-cdk-lib==2.121.1
constructs>=10.0.0,<11.0.0

# CDK CLI utilities (optional but recommended for development)
aws-cdk.cloud-assembly-schema==2.121.1

# Python type checking support (optional but recommended)
typing-extensions>=4.0.0

# Development and testing dependencies (optional)
pytest>=7.0.0
black>=22.0.0
flake8>=5.0.0
mypy>=1.0.0

# AWS SDK for Python (boto3) - useful for custom resources and testing
boto3>=1.26.0
botocore>=1.29.0

# Additional utilities for CDK development
jsii>=1.75.0
publication>=0.0.3
typeguard>=2.13.0

# Security and compliance scanning (optional)
safety>=2.0.0
bandit>=1.7.0

# Documentation generation (optional)
sphinx>=5.0.0
sphinx-rtd-theme>=1.0.0

# Requirements for specific AWS services used in this architecture
# These are included in aws-cdk-lib but listed here for clarity:
# - aws-cdk.aws-ec2 (VPC, Transit Gateway, Security Groups)
# - aws-cdk.aws-logs (CloudWatch Logs, VPC Flow Logs)
# - aws-cdk.aws-cloudwatch (CloudWatch Alarms, Metrics)
# - aws-cdk.aws-iam (IAM Roles, Policies)
# - aws-cdk.core (Stack, App, Environment, Tags)

# JSON schema validation for configuration files
jsonschema>=4.0.0

# YAML processing for configuration files
PyYAML>=6.0

# Rich console output for better development experience
rich>=12.0.0

# Environment variable management
python-dotenv>=0.19.0
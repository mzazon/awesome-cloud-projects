# AWS CDK Python Dependencies for URL Shortener Service
# This file specifies all the required Python packages for the CDK application

# Core AWS CDK library - provides the foundation for all CDK constructs
aws-cdk-lib==2.164.1

# Core constructs library - provides the base Construct class
constructs>=10.0.0,<11.0.0

# Additional useful packages for development and testing
pytest>=6.2.0,<8.0.0
pytest-cov>=2.12.0,<5.0.0

# Code formatting and linting tools (optional but recommended)
black>=21.0.0,<24.0.0
flake8>=3.9.0,<7.0.0
mypy>=0.910,<2.0.0

# AWS CLI for local testing and deployment validation
awscli>=1.25.0

# Python type stubs for boto3 (used in Lambda function)
boto3-stubs[dynamodb]>=1.26.0

# Development dependencies for local testing
moto[dynamodb]>=4.0.0  # For mocking AWS services in tests
requests>=2.25.0       # For testing HTTP endpoints
## List Conversations Response Mapping Template
## Returns paginated conversation results

#if($ctx.error)
    $util.error($ctx.error.message, $ctx.error.type)
#end

## Format response as a connection type with pagination support
{
    "items": $util.toJson($ctx.result.items),
    "nextToken": #if($ctx.result.nextToken) "$ctx.result.nextToken" #else null #end,
    "hasMore": #if($ctx.result.nextToken) true #else false #end
}
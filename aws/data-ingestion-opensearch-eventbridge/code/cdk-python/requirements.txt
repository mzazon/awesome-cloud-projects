# AWS CDK Python Dependencies for Data Ingestion Pipeline
# Core CDK framework and construct library
aws-cdk-lib==2.170.0
constructs>=10.0.0,<11.0.0

# Additional CDK modules for specific services
# (These are included in aws-cdk-lib for CDK v2, but listed for reference)

# Development and testing dependencies
boto3>=1.35.0
botocore>=1.35.0

# Type checking and code quality
mypy>=1.10.0
black>=24.0.0
flake8>=7.0.0
pylint>=3.0.0

# Testing framework
pytest>=8.0.0
pytest-cov>=4.0.0
moto>=4.2.0  # For mocking AWS services in tests

# Documentation generation
sphinx>=7.0.0
sphinx-rtd-theme>=2.0.0

# Security scanning
bandit>=1.7.0
safety>=3.0.0

# Requirements for data processing and validation
pyyaml>=6.0.0
jsonschema>=4.21.0

# AWS CLI for deployment scripts
awscli>=1.34.0
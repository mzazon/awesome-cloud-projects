# AWS CDK Dependencies for Text-to-Speech Solutions with Amazon Polly
# This file contains all required dependencies for the CDK Python application

# Core CDK library - main framework
aws-cdk-lib==2.162.1

# CDK constructs library - base constructs
constructs>=10.0.0,<11.0.0

# AWS service-specific CDK modules (included in aws-cdk-lib v2, but listed for clarity)
# These are automatically included with aws-cdk-lib but shown here for documentation

# Development and testing dependencies
pytest>=7.0.0
pytest-cov>=4.0.0
black>=22.0.0
flake8>=5.0.0
mypy>=1.0.0

# Type stubs for AWS SDK (boto3) - useful for development
boto3-stubs[polly,s3,lambda,iam,logs,cloudfront,apigateway,events]>=1.34.0

# Additional useful libraries for development
pydantic>=2.0.0  # For data validation if extending the application
python-dotenv>=1.0.0  # For environment variable management

# Documentation generation (optional)
sphinx>=5.0.0
sphinx-rtd-theme>=1.2.0

# Code quality tools
isort>=5.12.0
autopep8>=2.0.0

# AWS CLI for deployment (optional - can also use CDK CLI directly)
awscli>=1.32.0

# CDK specific requirements
jsii>=1.94.0
publication>=0.0.3
typeguard>=2.13.3

# For local testing and development
moto[polly,s3,lambda,iam]>=4.2.0  # AWS service mocking
requests>=2.31.0  # HTTP library for testing API endpoints

# Optional: Performance monitoring and observability
aws-lambda-powertools>=2.31.0  # For enhanced Lambda observability

# Version constraints to ensure compatibility
# Note: aws-cdk-lib includes most AWS service constructs
# The version should be kept in sync with the CDK CLI version
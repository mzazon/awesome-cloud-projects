# Python dependencies for QR Code Generator Lambda function
# These packages will be included in the Lambda deployment package

# QR code generation library with PIL support for image creation
qrcode[pil]==7.4.2

# PIL (Python Imaging Library) for image processing
# Included automatically with qrcode[pil] but specified for clarity
Pillow==10.1.0

# Boto3 is already available in the Lambda runtime environment
# but we can specify a version for local development consistency
# boto3==1.34.0
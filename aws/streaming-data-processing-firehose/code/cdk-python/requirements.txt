# AWS CDK Python Dependencies for Real-Time Data Processing
# This file specifies the Python packages required for the CDK application

# Core CDK library - main CDK framework
aws-cdk-lib>=2.110.0,<3.0.0

# CDK Constructs library - base constructs for CDK apps
constructs>=10.0.0,<11.0.0

# Optional: AWS CDK CLI for local development
# Note: Install globally with: npm install -g aws-cdk

# Optional: Python type checking (recommended for development)
# mypy>=1.0.0
# boto3-stubs[essential]>=1.34.0

# Optional: AWS Lambda Powertools for enhanced Lambda functions
# aws-lambda-powertools>=2.0.0

# Optional: Testing dependencies
# pytest>=7.0.0
# pytest-cov>=4.0.0
# moto>=4.0.0  # for mocking AWS services in tests

# Optional: Code formatting and linting
# black>=23.0.0
# flake8>=6.0.0
# isort>=5.0.0

# Note: This requirements.txt follows CDK v2 best practices
# CDK v2 consolidates all AWS service constructs into aws-cdk-lib
# No need for individual service packages (aws-cdk.aws-s3, etc.)
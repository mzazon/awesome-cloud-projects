# AWS CDK Python Requirements for Real-time Data Synchronization with AppSync
# This file specifies the Python packages required for deploying the CDK application

# Core AWS CDK library - provides base CDK functionality
aws-cdk-lib==2.162.1

# CDK Constructs library - provides reusable cloud infrastructure patterns
constructs>=10.0.0,<11.0.0

# Core Python dependencies for CDK applications
attrs>=23.1.0
cattrs>=23.1.2
importlib-resources>=5.10.0
jsii>=1.91.0
publication>=0.0.3
typeguard>=4.0.0

# Additional development dependencies for enhanced development experience
boto3>=1.34.0          # AWS SDK for Python for testing and validation
pytest>=7.4.0          # Testing framework for unit tests
pytest-cov>=4.1.0      # Coverage reporting for tests
black>=23.9.0          # Code formatter for consistent Python style
flake8>=6.1.0          # Linting tool for code quality
mypy>=1.6.0            # Static type checker for Python
bandit>=1.7.5          # Security linter for Python code

# Documentation and development tools
sphinx>=7.2.0          # Documentation generation
sphinx-rtd-theme>=1.3.0  # Read the Docs theme for Sphinx

# Optional: JSON schema validation for AppSync schemas
jsonschema>=4.19.0

# Optional: Enhanced logging and debugging
structlog>=23.2.0

# Note: Pin versions to ensure reproducible builds across environments
# Update versions periodically to get latest security patches and features
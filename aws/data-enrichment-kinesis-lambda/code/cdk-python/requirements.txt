# AWS CDK Python Dependencies for Data Enrichment Pipeline
# Core CDK library and AWS service constructs for streaming data enrichment

# Core CDK framework
aws-cdk-lib==2.165.0
constructs>=10.0.0

# Type checking and development tools
mypy==1.13.0
pytest==8.3.3
black==24.8.0

# Additional dependencies for development and testing
boto3==1.35.78
boto3-stubs[essential]==1.35.78
typing-extensions==4.12.2

# CDK CLI utilities (install globally with: npm install -g aws-cdk)
# aws-cdk (CLI tool - install via npm, not pip)

# Dependencies for Lambda function runtime (included in deployment package)
# Note: These are for local development/testing only
# The actual Lambda runtime will have boto3 available by default
# AWS CDK Python Requirements
# Core CDK dependencies for ordered message processing infrastructure

# Core CDK library
aws-cdk-lib==2.165.0

# Constructs library for building CDK applications
constructs>=10.0.0,<11.0.0

# Additional AWS service libraries (included in aws-cdk-lib v2)
# These are automatically included but listed for reference:
# - aws-cdk.aws-sqs
# - aws-cdk.aws-lambda
# - aws-cdk.aws-lambda-event-sources
# - aws-cdk.aws-dynamodb
# - aws-cdk.aws-s3
# - aws-cdk.aws-sns
# - aws-cdk.aws-cloudwatch
# - aws-cdk.aws-iam
# - aws-cdk.aws-logs

# Development and testing dependencies
pytest>=7.0.0
pytest-cov>=4.0.0
black>=22.0.0
flake8>=5.0.0
mypy>=1.0.0

# Type hints for boto3 (optional but recommended for development)
boto3-stubs[essential]>=1.26.0

# For JSON schema validation in Lambda functions
jsonschema>=4.0.0

# For enhanced datetime handling in Lambda functions  
python-dateutil>=2.8.0

# For decimal number handling in financial calculations
# (Decimal is built-in to Python, but listed for clarity)
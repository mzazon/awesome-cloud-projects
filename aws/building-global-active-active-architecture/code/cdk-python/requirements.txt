# AWS CDK Python dependencies for Multi-Region Active-Active Applications with Global Accelerator
# Core CDK library
aws-cdk-lib==2.164.1

# CDK constructs library
constructs>=10.0.0,<11.0.0

# AWS service-specific CDK libraries (included in aws-cdk-lib v2)
# Core CDK dependencies for multi-region active-active architecture

# Development and testing dependencies
pytest>=6.0.0
pytest-cov>=2.10.0
black>=21.0.0
flake8>=3.8.0
mypy>=0.812

# Type stubs for better IDE support
boto3-stubs[dynamodb,lambda,elbv2,globalaccelerator,iam,ec2]>=1.34.0

# AWS CLI for deployment (optional, but recommended)
awscli>=1.32.0

# Additional utilities
python-dotenv>=0.19.0  # For environment variable management
pydantic>=1.8.0  # For configuration validation
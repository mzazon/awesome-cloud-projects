# AWS CDK Python Dependencies for Simple Text Processing with CloudShell and S3
# This file specifies the Python packages required for the CDK application

# Core AWS CDK library - provides the fundamental CDK framework
aws-cdk-lib>=2.150.0

# CDK Constructs library - provides reusable cloud components
constructs>=10.0.0

# Development and testing dependencies
pytest>=6.2.0
pytest-cdk>=1.0.0

# Code quality and formatting tools
black>=22.0.0
flake8>=4.0.0
mypy>=0.991

# Type stubs for AWS SDK (optional but recommended for better IDE support)
boto3-stubs[essential]>=1.28.0

# Additional utility libraries that might be useful for text processing workflows
# (These are optional and can be removed if not needed)
pandas>=1.5.0
numpy>=1.24.0

# For generating sample data files (optional)
faker>=18.0.0

# For working with different file formats (optional)
python-dotenv>=1.0.0

# Security scanning (optional but recommended)
bandit>=1.7.5

# Documentation generation (optional)
sphinx>=5.0.0
sphinx-rtd-theme>=1.2.0

# Note: Pin versions for production use
# The >= versions allow for compatible updates while maintaining stability
# For production deployments, consider using exact versions (==) instead
# CDK Python Dependencies for AWS Systems Manager State Manager Recipe
# 
# This file specifies the Python dependencies required to deploy the
# AWS Systems Manager State Manager configuration management solution.

# Core CDK dependencies
aws-cdk-lib==2.114.1
constructs>=10.0.0,<11.0.0

# AWS CDK service-specific libraries (already included in aws-cdk-lib v2)
# These are listed for reference but not needed as separate dependencies

# Development and testing dependencies
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.5.0
boto3>=1.34.0
botocore>=1.34.0

# Type checking for AWS CDK
types-boto3>=1.0.0
# Lambda Layer Dependencies for Sustainability Data Processing
# These packages are optimized for AWS Lambda ARM64 (Graviton) architecture

# Core data processing
pandas>=2.1.0,<3.0.0
numpy>=1.24.0,<2.0.0

# AWS SDK (boto3) is provided by AWS Lambda runtime
# boto3>=1.34.0  # Commented out as it's included in Lambda runtime

# JSON and data manipulation
python-dateutil>=2.8.0
pytz>=2023.3

# Optional: Scientific computing (only if heavy numerical analysis needed)
# scipy>=1.11.0  # Uncomment only if advanced statistical analysis required

# Development and testing dependencies (not needed in Lambda)
# pytest>=7.4.0
# moto>=4.2.0  # For AWS service mocking in tests
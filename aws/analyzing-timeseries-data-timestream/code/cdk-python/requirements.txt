# AWS CDK Python dependencies for Amazon Timestream Time-Series Data Solution
# Core CDK libraries
aws-cdk-lib==2.151.0
constructs>=10.0.0,<11.0.0

# Python dependencies for CDK application
boto3>=1.26.0
typing-extensions>=4.0.0

# Development and testing dependencies (optional)
pytest>=7.0.0
pytest-mock>=3.10.0
black>=22.0.0
flake8>=5.0.0
mypy>=1.0.0

# Timestream specific dependencies for Lambda functions
# Note: These are included in the Lambda runtime, but listed for reference
# boto3 (included in Lambda runtime)
# botocore (included in Lambda runtime)

# Additional AWS SDK dependencies for extended functionality
aws-sdk-pandas>=3.0.0  # For advanced data processing if needed
aws-lambda-powertools>=2.0.0  # For enhanced Lambda logging and monitoring

# Type stubs for better IDE support
boto3-stubs[timestream-write,timestream-query,iot,lambda,s3,cloudwatch,iam]>=1.26.0
types-requests>=2.28.0
# AWS CDK Python Requirements for Simple Password Generator
# This file specifies the Python dependencies needed for the CDK application

# Core CDK library - always specify exact version for reproducible builds
aws-cdk-lib==2.166.0

# CDK Constructs library - the base constructs framework
constructs>=10.0.0,<11.0.0

# Additional useful libraries for development and testing

# Type checking support
typing-extensions>=4.0.0

# JSON handling (usually included with Python, but explicit for clarity)
# Standard library - no need to install separately

# Development dependencies (optional - uncomment if needed)
# pytest>=7.0.0
# pytest-cov>=4.0.0  
# black>=22.0.0
# flake8>=5.0.0
# mypy>=0.991

# AWS SDK for Python (boto3) - used by Lambda function, but not needed for CDK
# boto3>=1.26.0

# Security note: Always pin versions in production to ensure reproducible builds
# Consider using pip-tools or poetry for better dependency management

# Version compatibility notes:
# - aws-cdk-lib 2.166.0 is compatible with CDK v2
# - constructs 10.x is the current stable version for CDK v2
# - Python 3.8+ is required for CDK v2

# To install these dependencies:
# pip install -r requirements.txt

# To upgrade CDK to latest version:
# pip install --upgrade aws-cdk-lib

# For development environment setup:
# python -m venv .venv
# source .venv/bin/activate  # On Windows: .venv\Scripts\activate
# pip install -r requirements.txt
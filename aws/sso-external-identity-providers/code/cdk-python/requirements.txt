# AWS CDK v2 Python Dependencies for IAM Identity Center (SSO) with External Identity Providers
# 
# Core CDK library and AWS service constructs required for implementing
# AWS Single Sign-On with external identity provider integration

# Core CDK framework
aws-cdk-lib>=2.161.0,<3.0.0

# CDK construct library
constructs>=10.0.0,<11.0.0

# Additional AWS service libraries (included in aws-cdk-lib for CDK v2)
# Listed here for documentation purposes - these are part of aws-cdk-lib:
# - aws-cdk.aws-identitystore
# - aws-cdk.aws-iam
# - aws-cdk.aws-organizations  
# - aws-cdk.aws-s3
# - aws-cdk.aws-sso
# - aws-cdk.aws-ssoadmin

# Development and testing dependencies
boto3>=1.34.0,<2.0.0
botocore>=1.34.0,<2.0.0

# Code quality and formatting tools
black>=23.0.0,<25.0.0
flake8>=6.0.0,<8.0.0
mypy>=1.8.0,<2.0.0

# Testing frameworks
pytest>=7.4.0,<9.0.0
pytest-cov>=4.1.0,<6.0.0
moto>=4.2.0,<6.0.0

# Type stubs for better IDE support
types-boto3>=1.0.0
boto3-stubs[identitystore,iam,organizations,s3,sso,sso-admin]>=1.34.0,<2.0.0

# YAML parsing for configuration files
PyYAML>=6.0.0,<7.0.0

# JSON schema validation
jsonschema>=4.17.0,<5.0.0

# HTTP requests for external API calls
requests>=2.31.0,<3.0.0

# Environment variable management
python-dotenv>=1.0.0,<2.0.0

# AWS CLI for deployment scripts
awscli>=1.32.0,<2.0.0
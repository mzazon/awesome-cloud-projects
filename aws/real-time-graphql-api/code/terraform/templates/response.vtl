## Generic response template for handling errors and returning results
## This template is used by multiple resolvers to handle DynamoDB responses

#if($ctx.error)
    $util.error($ctx.error.message, $ctx.error.type)
#end

## Return the result from DynamoDB
$util.toJson($ctx.result)